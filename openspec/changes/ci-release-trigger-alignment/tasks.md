## 1. OpenSpec Foundation

- [x] 1.1 Author the proposal, design, tasks, task journal, and spec delta for CI/release trigger alignment

## 2. Validation Tooling

- [x] 2.1 Add a repo validation script that checks all active OpenSpec changes and update bootstrap-dev to use it

## 3. GitHub Actions

- [x] 3.1 Add a CI workflow for pushes to `main`, pull requests, and manual runs
- [x] 3.2 Correct the release workflow trigger so semver tags like `v0.1.0` run the release pipeline

## 4. Version Surface And Verification

- [x] 4.1 Align release-facing examples/comments around `0.1.0` / `v0.1.0`
- [x] 4.2 Run formatting, linting, tests, OpenSpec validation, and task-journal validation, then record the results
