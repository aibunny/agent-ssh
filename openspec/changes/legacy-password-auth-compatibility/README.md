# legacy-password-auth-compatibility

Introduce an explicit, opt-in legacy password-auth compatibility lane that keeps plaintext password material out of agent-visible configuration, CLI arguments, audit logs, and review artifacts.
